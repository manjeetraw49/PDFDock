{
  "name": "pdfdock",
  "version": "1.0.0",
  "description": "PDFDock - Fast, 100% In-Browser Client-Side PDF Tools",
  "scripts": {
    "build": "python3 build_pages.py || python build_pages.py || echo 'Prebuilt static files ready'",
    "start": "python3 server.py 8080 || python server.py 8080"
  }
}
